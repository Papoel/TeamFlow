<?xml version="1.0"?>
<ruleset name="TeamFlow PHPMD Rules"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>Custom PHPMD rules for TeamFlow project</description>

    <!-- Clean Code Rules -->
    <rule ref="rulesets/cleancode.xml">
        <exclude name="StaticAccess"/>
        <exclude name="BooleanArgumentFlag"/>
    </rule>

    <!-- Code Size Rules -->
    <rule ref="rulesets/codesize.xml"/>

    <!-- Controversial Rules -->
    <rule ref="rulesets/controversial.xml"/>

    <!-- Design Rules -->
    <rule ref="rulesets/design.xml"/>

    <!-- Naming Rules -->
    <rule ref="rulesets/naming.xml">
        <exclude name="ShortVariable"/>
    </rule>

    <!-- Custom ShortVariable rule with Doctrine support -->
    <rule ref="rulesets/naming.xml/ShortVariable">
        <properties>
            <!-- Minimum length for variable names (2 allows $id, $em, etc.) -->
            <property name="minimum" value="2"/>
            <!-- Exceptions for common short names in Doctrine/Symfony -->
            <property name="exceptions" value="id,em,io,to,db"/>
        </properties>
    </rule>

    <!-- Unused Code Rules -->
    <rule ref="rulesets/unusedcode.xml"/>
</ruleset>
